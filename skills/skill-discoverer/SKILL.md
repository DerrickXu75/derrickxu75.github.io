---
name: skill-discoverer
description: 自动发现、推荐和安装 ClawHub 新技能。定期搜索热门和最新 skills，过滤后推荐给用户，确认后自动安装。
---

# Skill Discoverer - 技能发现者

自动搜索、推荐和安装 ClawHub 上的新技能，持续提升主 Agent 能力。

## 使用方式

### 手动触发搜索

```bash
python3 ~/.openclaw/workspace/skills/skill-discoverer/scripts/discover_skills.py
```

输出 JSON 格式的推荐列表，包含：
- 技能信息（名称、描述、版本）
- 统计数据（下载量、安装量）
- 推荐理由

### 安装技能

```bash
# 安装技能
python3 ~/.openclaw/workspace/skills/skill-discoverer/scripts/install_skill.py <slug>

# 查看技能详情（不安装）
python3 ~/.openclaw/workspace/skills/skill-discoverer/scripts/install_skill.py <slug> --inspect

# 检查是否需要配置
python3 ~/.openclaw/workspace/skills/skill-discoverer/scripts/install_skill.py <slug> --check-config
```

### 作为 Sub-Agent 运行

```bash
openclaw sessions_spawn \
  --label "skill-discoverer" \
  --mode "session" \
  --task "运行技能发现者，搜索并推荐新 skills"
```

---

## 工作流程

```
1. 定期搜索（每 2 小时）
   └─ clawhub explore --sort newest
   └─ clawhub search <keywords>

2. 过滤筛选
   ├─ 跳过已安装/已推荐
   ├─ 最低下载量要求
   ├─ 版本检查
   └─ 质量评分

3. 推荐给用户
   └─ 飞书发送推荐卡片
   └─ 等待用户确认

4. 用户确认后
   ├─ 自动安装 clawhub install
   ├─ 检查是否需要配置
   └─ 如需配置，通知用户

5. 记录状态
   └─ 更新 installed.json
   └─ 更新 recommended.json
```

---

## 配置文件

### CONFIG.md

定义搜索策略：
- 兴趣领域关键词
- 过滤规则
- 推荐流程

### 状态文件（state/）

- `installed.json` - 已安装的技能列表
- `recommended.json` - 已推荐过的技能列表

---

## 搜索关键词

按优先级分类：

| 类别 | 关键词 |
|------|--------|
| 效率工具 | automation, productivity, workflow |
| 数据分析 | data, analysis, chart, visualization |
| 开发工具 | git, github, code, debug, testing |
| AI 增强 | llm, ai, prompt, rag, embedding |
| 系统集成 | api, webhook, notification, calendar, email |
| 安全 | security, audit, permission, auth |
| 运维 | backup, monitoring, healthcheck |

---

## 推荐格式

```
━━━━━━━━━━━━━━━━━━━━
🆕 新 Skill 推荐

📦 名称：Skill 名称
🏷️  ID：`skill-slug`

📝 描述：
技能描述...

📊 统计：
   • 版本：1.0.0
   • 下载：123
   • 安装：50
   • 评分：⭐⭐⭐⭐

💡 推荐理由：热门 (123 下载) | 匹配：github

━━━━━━━━━━━━━━━━━━━━
回复「安装」自动安装此技能
回复「忽略」跳过此技能
回复「详情」查看完整信息
```

---

## 与主 Agent 集成

### 在 HEARTBEAT.md 中添加

```markdown
- 每 2 小时运行 skill-discoverer 搜索新技能
- 发现高分技能时通知用户
- 用户确认后自动安装
```

### 在主对话中触发

用户说：
- "找找有什么新技能"
- "看看有没有好用的 GitHub 工具"
- "更新一下技能库"

→ 主 Agent spawn skill-discoverer 执行搜索

---

## 最佳实践

### ✅ 推荐
- 下载量 > 20 的技能
- 有清晰使用说明的
- 版本 >= 0.1.0 的
- 有 changelog 的

### ⚠️ 谨慎
- 刚发布 (<24 小时) 的技能
- 下载量 < 5 的技能
- 版本 < 0.0.1 的（太早期）
- 没有文档的

### ❌ 跳过
- 已安装或已推荐过的
- 描述太短 (<20 字符)
- 与现有技能功能重复的

---

## 扩展建议

1. **用户反馈循环**
   - 记录用户对推荐的反应
   - 调整推荐算法

2. **技能分类**
   - 按功能分类推荐
   - 优先推荐互补技能

3. **自动测试**
   - 安装后自动验证技能可用
   - 失败则回滚

4. **依赖管理**
   - 检测技能间的依赖关系
   - 批量安装相关技能

---

## 故障排除

### 搜索失败
```bash
# 检查网络连接
clawhub explore --json

# 检查 clawhub 是否登录
clawhub whoami
```

### 安装失败
```bash
# 检查权限
ls -la ~/.openclaw/workspace/skills/

# 手动安装测试
clawhub install <slug> --workdir ~/.openclaw/workspace
```

### 重复推荐
```bash
# 清理状态文件
rm ~/.openclaw/workspace/skills/skill-discoverer/state/recommended.json
```

---

## 相关文件

- `CONFIG.md` - 搜索策略配置
- `scripts/discover_skills.py` - 搜索脚本
- `scripts/install_skill.py` - 安装脚本
- `state/installed.json` - 已安装记录
- `state/recommended.json` - 已推荐记录
